{% extends 'playground.html.twig' %}

{% set heading = 'List Layout' %}

{%
    set breadcrumbs = [
        {'label': 'Pulsar', 'href': '#'},
        {'label': 'Examples', 'href': '#'},
        {'label': 'List' }
    ]
%}

{% set tab_one %}
    {% include 'examples/list/tab.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "one",
          "label" : "Tab One",
          "src"   : tab_one,
          "active": true
        }
    ]
%}
