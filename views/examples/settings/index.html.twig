{% extends 'playground.html.twig' %}

{% set heading = 'Settings Layout' %}

{%
    set breadcrumbs = [
        {'label': 'Pulsar', 'href': '#'},
        {'label': 'Examples', 'href': '#'},
        {'label': 'Settings' }
    ]
%}

{% set tab_one %}
    {% include 'examples/settings/tab.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "one",
          "label" : "Tab One",
          "src"   : tab_one,
          "active": true
        }
    ]
%}
