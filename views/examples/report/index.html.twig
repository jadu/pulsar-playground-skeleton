{% extends 'playground.html.twig' %}

{% set heading = 'Report Layout' %}

{%
    set breadcrumbs = [
        {'label': 'Pulsar', 'href': '#'},
        {'label': 'Examples', 'href': '#'},
        {'label': 'Report' }
    ]
%}

{% set tab_one %}
    {% include 'examples/report/tab.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "one",
          "class" : "ui-report",
          "label" : "Tab One",
          "src"   : tab_one,
          "active": true
        }
    ]
%}
