{% extends 'playground.html.twig' %}

{% set heading = 'List Layout' %}

{%
    set breadcrumbs = [
        {'label': 'Pulsar', 'href': '#'},
        {'label': 'Layouts', 'href': '#'},
        {'label': 'List' }
    ]
%}

{% set tab_one %}
    {% include 'list/tab.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "one",
          "label" : "Tab One",
          "src"   : tab_one,
          "active": true
        }
    ]
%}
