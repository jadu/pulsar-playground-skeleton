{% extends 'playground.html.twig' %}

{% set heading = 'My Playground' %}

{%
    set breadcrumbs = [
        {'label': 'Pulsar', 'href': '#'},
        {'label': 'Playground', 'href': '#'},
        {'label': 'My Playground' }
    ]
%}

{% set tab_one %}
    {% include 'playground/tab.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "one",
          "label" : "Tab One",
          "src"   : tab_one,
          "active": true
        }
    ]
%}
