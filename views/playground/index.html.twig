{% extends 'playground.html.twig' %}
{% import '@pulsar/pulsar/v2/helpers/html.html.twig' as html %}

{% set heading = 'Continuum Festival ' ~ html.label({'label': 'unpublished', 'class': 'label--warning'}) %}

{%
    set breadcrumbs = [
        {'label': 'Pulsar', 'href': '#'},
        {'label': 'Playground', 'href': '#'},
        {'label': 'My Playground' }
    ]
%}

{% set tab_one %}
    {% include 'playground/tab.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "settings",
          "label" : "Settings"
        },
        {
          "id"    : "theme",
          "label" : "Theme",
          "src"   : tab_one,
          "active": true
        },
        {
          "id"    : "security",
          "label" : "Security"
        },
        {
          "id"    : "admin",
          "label" : "Admin"
        }
    ]
%}
